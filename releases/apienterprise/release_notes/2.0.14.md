- Fix for socket management deadlocks
- If replication fails for an ENVKEY, don't stop the whole replication operation. Log the error and keep going with any additional ENVKEYs that need to be replicated.
