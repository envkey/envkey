Nevermind on the 3.0.0 breaking change. Instead, fix the updates issue while still retaining exports to subsequent buildpacks by keeping `$BP_DIR/export`, but removing `.profile.d` export to application.
